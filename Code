from itertools import permutations
import urllib.request

response = urllib.request.urlopen('https://drive.google.com/file/d/1bbAmbo7NIBPGRgvmHLRu9PJpbFprBHpB/view?usp=sharing')
Dictionary = response.read()
User_input = input("Enter a word of your choice : ")
i=0
pe_list=[]
z=len(items)
for L in range(1,z+1):
	perms = [''.join(p) for p in permutations(User_input,L)]

	pe_list=pe_list+perms
	l=l+1

#Final= nltk.wordnet.synsets(pe_list)
#print(Final)
#print(set(perms).intersection(text))

for p in pe_list:
	for T in Dictionary:
		if(p==T):
			print(T)
